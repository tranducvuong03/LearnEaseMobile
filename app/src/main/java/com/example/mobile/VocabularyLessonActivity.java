package com.example.mobile;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class VocabularyLessonActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.card_vocabulary_lesson); // Đặt layout của card từ vựng
        // Thêm logic cụ thể cho bài học từ vựng ở đây
        // Ví dụ: thiết lập RecyclerView để hiển thị danh sách từ vựng, xử lý tương tác người dùng, v.v.
    }
}