package com.example.mobile; // Thay thế bằng package của bạn

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class TheoryLessonActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.card_theory_lesson); // Đặt layout của card lý thuyết
        // Thêm logic cụ thể cho bài học lý thuyết ở đây
        // Ví dụ: hiển thị nội dung lý thuyết, thêm các ví dụ, bài tập, v.v.
    }
}